# PRD 05 - Cloud exposure

Support public HTTPS tunnel endpoints through private TOML settings without changing
the signed rules. Acceptance: two remote machines can exchange MCP messages and a
timeout produces a defined technical result. Actual endpoint values are negotiated
per opponent and must not be hard-coded in the repository.
