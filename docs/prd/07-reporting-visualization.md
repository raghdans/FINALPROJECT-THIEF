# PRD 07 - Reporting and visualization

Provide local-state GUI, replay verification, standardized declaration/config/log/
result JSON and optional Gmail reporting. Acceptance: a six-game series produces
all artifacts with one shared game UID and the replay reports `VERIFIED OK`.
