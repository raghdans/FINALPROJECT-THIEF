# Development plan

1. Implement and test board physics locally.
2. Separate the peers and prove FastMCP transport on localhost.
3. Inject deterministic role strategies behind the strategy interface.
4. Fuse scent observations into a belief map and constrain verbal hints.
5. configure public tunnels only after localhost reliability is proven.
6. Seal every step with SHA-256 commit-reveal and run mutual audit.
7. Emit the four JSON artifact types, render GUI/replay evidence, and prepare submission.

Every stage must pass its unit and end-to-end checks before the next stage is accepted.
